import React from 'react';

import * as S from './styles';

const Loader: React.FC = () => {
  return (
    <S.LoaderContainer>
      <S.Loader />
    </S.LoaderContainer>
  );
};

export default Loader;
